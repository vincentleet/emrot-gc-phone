# Call audio

Place your pre-recorded call audio file here as `call.mp3` (or `.ogg` for broader support).

The app will play this during the active call when the player taps "Answer" on the incoming call.
